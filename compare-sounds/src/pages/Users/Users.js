import React from 'react';
import { Card, CardContent } from '@material-ui/core';

export default function Users () {
  return (
    <Card>
      <CardContent>
        Users
      </CardContent>
    </Card>
  )
}